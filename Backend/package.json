{
  "name": "backend",
  "version": "1.0.0",
  "description": "Banking Helper AI Backend using Groq",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "bcryptjs": "^3.0.3",
    "build": "^0.1.4",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "express-rate-limit": "^8.2.1",
    "groq-sdk": "^0.37.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.6.0",
    "multer": "^2.0.2",
    "pdf-parse": "^2.4.5",
    "run": "^1.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
